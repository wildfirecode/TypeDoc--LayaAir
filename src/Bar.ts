namespace baz {
    /**
     * Comment for class Bar.
     * @author someone
     * @version 1.0.0
     */
    export class Bar2 {
        /**
         * Comment for method foo.
         * @param target Comment for param target.
         * @param arg Comment for param arg.
         * @return Comment for return
         */
        foo(target: Laya.Sprite, arg: number): number {
            return 0
        }
    }
}